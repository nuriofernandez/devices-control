info:
  name: / (list)
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8000
  auth:
    type: bearer
    token: "{{AUTH}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
