info:
  name: /id (status)
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8000/{{DEVICE_ID}}
  auth:
    type: bearer
    token: "{{AUTH}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
