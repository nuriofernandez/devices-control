info:
  name: /id/off (turnoff)
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8000/{{DEVICE_ID}}/off
  auth:
    type: bearer
    token: "{{AUTH}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
